#include "Monthy.h"
/**


*/
int is_digit(void)
{
    char *argv;
    int i = 0;

    while (arvg[i] != '\0')
    {
        if (isdigit(argv[i] == 0))
            return (1);
    i++;
    }
    return (0);
}
